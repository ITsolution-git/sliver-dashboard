(function () {
    'use strict';

    angular
        .module('adminapp')
        .run(runApp);

    function runApp($rootScope, $timeout, $window, $state, $auth, BCService, CONFIG, userService) {

    }

})();