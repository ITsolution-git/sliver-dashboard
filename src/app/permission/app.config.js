(function () {
    'use strict';

    angular
        .module('slapperm')
        .config(config);

    /* @ngInject */
    function config($stateProvider, $urlRouterProvider, $authProvider, CONFIG) {


    }
})();